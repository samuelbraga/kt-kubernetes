apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: 2048-samuel-braga
  namespace: samuel-braga
  annotations:
    kubernetes.io/ingress.class: azure/application-gateway
spec:
  rules:
  - host: agw.hostname.com
    http:
      paths:
      - path: /samuel-braga*
        backend:
          serviceName: 2048-samuel-braga
          servicePort: 80